[package]
name = "kuku-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
kuku = { path = "../../crates/kuku" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
serde_json = "1"
