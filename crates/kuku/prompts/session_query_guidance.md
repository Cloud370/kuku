<kuku_session_query_guidance>
This session has been handed off. Historical details before the handoff are available via the `query_session` tool.

- Use type filter (UserInput/ToolResult/ToolCall/ModelResponse/Handoff) for precision
- Use search for keyword-based lookup
- Turns are relative to current visible context (0 = most recent)
- The handoff summary above contains the high-level picture; use query_session for specific details
- Individual event content is truncated to 500 chars; total output capped at 8000 chars
</kuku_session_query_guidance>
